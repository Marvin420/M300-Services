import time

import redis
from flask import Flask


def get_hit_count():
    retries = 5
    while True:
        try:
            return cache.incr('hits')
        except redis.exceptions.ConnectionError as exc:
            if retries == 0:
                raise exc
            retries -= 1
            time.sleep(0.5)

@app.route('/')
def hello():
    count = get_hit_count()
    return 'Grüezi Herr Berger! Diese Seite wurde {} mal aktualisiert.\n'.format(count)
